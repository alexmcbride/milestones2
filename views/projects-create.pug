extends layout

block content
  h1 Create Project
  p Enter a name to create a new project
  form(method='post')
    div.form-group
        label(for='name') Name
        input.form-control(type='text', name="name" id='name')
    div.text-right
        button.btn.btn-primary(type='submit') Create Project
